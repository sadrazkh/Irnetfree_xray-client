<div align="center">

# 🛡️ IRNetFree

**یک کلاینت دسکتاپ تمیز، مدرن و امن برای اجرای کانفیگ‌های Xray**
پروکسی • تانل کل‌سیستم (TUN) • روتینگ پیشرفته • زنجیره پروکسی • تست پینگ • بررسی IP

A clean, modern, cross‑platform desktop **VPN client** built on **Electron**, driving **xray‑core**.
Windows · macOS (Intel + Apple Silicon) · Linux

</div>

---

## 📑 فهرست (Table of Contents)

- [امکانات کامل](#-امکانات-کامل-features)
- [نصب و اجرا](#-نصب-و-اجرا-setup)
- [شروع سریع](#-شروع-سریع-quick-start)
- [افزودن کانفیگ‌ها](#-افزودن-کانفیگها-adding-configs)
- [ساب‌اسکریپشن](#-ساباسکریپشن-subscriptions)
- [حالت اتصال: پروکسی یا TUN](#-حالت-اتصال-پروکسی-یا-tun-connection-mode)
  - [لایه‌ی تونل: sing-box (یا tun2socks)](#لایهی-تونل-sing-box-یا-tun2socks)
  - [گارد نشتی (Leak Guard)](#گارد-نشتی-leak-guard)
  - [بستن UDP در حالت پروکسی](#بستن-udp-در-حالت-پروکسی)
- [روتینگ](#-روتینگ-routing)
  - [روتینگ ساده](#۱-روتینگ-ساده-simple)
  - [قوانین سفارشی](#۲-قوانین-سفارشی-custom-rules)
  - [روتینگ پیشرفته گرافیکی](#۳-روتینگ-پیشرفته-گرافیکی-advanced)
  - [روتینگ بر اساس پروسه](#۴-روتینگ-بر-اساس-پروسه-process)
- [زنجیره پروکسی](#-زنجیره-پروکسی-proxy-chains)
- [استخر پروکسی (چند کانفیگ هم‌زمان)](#-استخر-پروکسی-multi-proxy-pool)
- [DNS](#-dns)
- [اشتراک در شبکه محلی (LAN)](#-اشتراک-در-شبکه-محلی-lan)
- [کیل‌سوییچ](#-کیلسوییچ-kill-switch)
- [اجرای با دسترسی ادمین](#-اجرای-با-دسترسی-ادمین-run-as-admin)
- [اولین اجرا روی مک](#-اولین-اجرا-روی-مک-macos--first-run)
- [فایل‌های موردنیاز](#-فایلهای-موردنیاز-required-files)
- [نسخه و به‌روزرسانی](#-نسخه-و-بهروزرسانی-version--update)
- [آمار و تست](#-آمار-و-تست-stats--testing)
- [ظاهر و رابط کاربری](#-ظاهر-و-رابط-کاربری-ui)
- [مرجع تنظیمات](#-مرجع-تنظیمات-settings-reference)
- [ساخت نسخه نصبی](#-ساخت-نسخه-نصبی-build)
- [نسخه CLI / سرور بدون GUI (لینوکس)](#-نسخه-cli--سرور-بدون-gui-لینوکس-headless)
- [نسخه اندروید](#-نسخه-اندروید-android)
- [انتشار خودکار (GitHub Actions)](#-انتشار-خودکار-github-actions-release)
- [ساختار پروژه](#-ساختار-پروژه-project-structure)
- [امنیت](#-امنیت-security)
- [عیب‌یابی](#-عیبیابی-troubleshooting)

---

## ✨ امکانات کامل (Features)

| دسته | توضیح |
|---|---|
| **پروتکل‌ها** | VLESS (با Reality / XTLS‑Vision)، VMess، Trojan، Shadowsocks، **WireGuard**، و **SOCKS5 / HTTP پروکسی** |
| **دو هسته‌ی Xray** | هسته‌ی رسمی (`XTLS/Xray-core`) و fork پترنیها (`patterniha/Xray-core`، کانفیگ‌های بدون TLS به آدرس عمومی را رد نمی‌کند). انتخاب per-config در فرم ویرایش، «هسته‌ی پیش‌فرض» در تنظیمات، هر دو از «فایل‌های موردنیاز» دانلود/به‌روزرسانی می‌شوند. اگر هسته‌ی رسمی کانفیگی را با خطای «without TLS» رد کند و PattN نصب باشد، خودکار روی PattN اجرا می‌شود. |
| **افزودن کانفیگ** | لینک تکی، چند لینک هم‌زمان، متن base64، لینک ساب‌اسکریپشن، فرم دستی WireGuard، **فرم دستی SOCKS/HTTP**، لینک `socks://`، و **چسباندن سریع با Ctrl+V** در هر جای برنامه |
| **ساب‌اسکریپشن** | افزودن، به‌روزرسانی دستی/خودکار، و نمایش **حجم مصرف و زمان باقی‌مانده با پروگرس‌بار** |
| **پروکسی محلی** | SOCKS5 + HTTP inbound با پورت‌های قابل تنظیم |
| **استخر پروکسی (Multi‑Proxy)** | اجرای **چند کانفیگ/زنجیره/ساکس هم‌زمان**، هرکدام روی پورت محلی جدا (مثلاً 60001، 60002 …) — سازگار با هر دو حالت پروکسی و TUN |
| **پروکسی سیستمی** | تنظیم خودکار پروکسی سیستم‌عامل هنگام اتصال |
| **حالت TUN** | تونل کردن **کل ترافیک سیستم** با sing-box (یا tun2socks به‌عنوان جایگزین قدیمی) روی Windows/macOS/Linux |
| **گارد نشتی (Leak Guard)** | در حالت TUN: DNS همه‌ی کارت‌های شبکه از تونل می‌رود (سطح استاندارد) و در سطح سخت‌گیرانه هر چیزی که از تونل نمی‌رود بسته می‌شود؛ در حالت پروکسی، گزینه‌ی بستن UDP جلوی لو رفتن IP با WebRTC را می‌گیرد |
| **روتینگ** | ساده (گلوبال/دور زدن ایران/چین/مستقیم) + قوانین سفارشی + **روتینگ پیشرفته گرافیکی** (که می‌تواند حالت ساده را هم زیرِ قانون‌هایش اعمال کند) |
| **روتینگ پراسس** | هدایت ترافیک یک **برنامه‌ی خاص** به یک سرور/زنجیره دلخواه |
| **زنجیره پروکسی** | عبور ترافیک از چند سرور پشت‌سرهم (مثلاً برای دسترسی به دیتابیس از طریق WireGuard) |
| **DNS** | DNS خارجی روی DoH از داخل تونل + DNS داخلی (شکن/الکترو…) برای «دور زدن ایران»؛ هر DNS ای که به هسته برسد همان‌جا جواب داده می‌شود |
| **اشتراک LAN** | تبدیل این دستگاه به پروکسی برای بقیه دستگاه‌های شبکه |
| **کیل‌سوییچ** | قطع کامل اینترنت در صورت افت ناگهانی اتصال (جلوگیری از نشتی) |
| **به‌روزرسانی** | بررسی نسخه‌ی جدید برنامه از گیت‌هاب + نمایش نسخه‌ی هسته‌ی Xray |
| **تست** | پینگ TCP، **تأخیر واقعی** از داخل تونل، و بررسی IP/کشور خروجی |
| **آمار زنده** | سرعت و حجم دانلود/آپلود لحظه‌ای |
| **ظاهر** | تم دارک، رابط دو‌زبانه (فارسی RTL / English)، رنگ‌بندی وضعیت اتصال، نشان روی آیکون تسک‌بار، سینی سیستم (tray) |
| **نسخه‌ی سرور (Headless/CLI)** | اجرای همین برنامه روی **لینوکسِ بدون GUI**؛ همان UI روی یک پورت وب بالا می‌آید (نصب تک‌خطی، دسترسی با SSH-forward) — [راهنما](#-نسخه-cli--سرور-بدون-gui-لینوکس-headless) |

---

## 🚀 نصب و اجرا (Setup)

**پیش‌نیاز:** Node.js 18+

```bash
git clone https://github.com/sadrazkh/Irnetfree_xray-client.git
cd Irnetfree_xray-client      # یا پوشه‌ی xray-client
npm install

# دانلود خودکار آخرین هسته xray-core در پوشه‌ی bin/
npm run get-xray
# یا: از داخل برنامه → تنظیمات → فایل‌های موردنیاز → دانلود

npm start
```

> نیازی به نصب دستی هسته نیست؛ اگر چیزی کم باشد، **هنگام باز شدن برنامه** پنجره‌ای ظاهر می‌شود و با اجازه‌ی شما فایل‌های لازم را دانلود می‌کند.

---

## ⚡ شروع سریع (Quick Start)

۱. برنامه را باز کنید؛ اگر هسته‌ی Xray نصب نیست، روی **«دانلود کن»** بزنید.
۲. به تب **سرورها** بروید و کانفیگ خود را اضافه کنید (یا هرجای برنامه **Ctrl+V** بزنید).
۳. به صفحه‌ی **اتصال** برگردید، از منوی بازشو سرور را انتخاب کنید.
۴. دکمه‌ی بزرگ روشن/خاموش وسط را بزنید.
۵. وقتی **تولبار و لوگو سبز** شد یعنی متصل‌اید ✅ — برای اطمینان «بررسی IP» را بزنید.

---

## 🔗 افزودن کانفیگ‌ها (Adding Configs)

در تب **سرورها** روی «+ افزودن» بزنید و یکی از موارد زیر را بچسبانید (برنامه نوعش را خودکار تشخیص می‌دهد):

**۱) لینک تکی:**
```
vless://uuid@example.com:443?security=reality&pbk=...&sni=...#MyServer
```

**۲) چند لینک با هم (هر خط یک کانفیگ):**
```
vless://...#Server-1
vmess://...
trojan://...@host:443#Server-3
ss://...#Shadowsocks-1
```

**۳) متن base64 (خروجی خام ساب):** کل بلوک base64 را بچسبانید، خودکار دیکد و وارد می‌شود.

**۴) لینک ساب‌اسکریپشن:** هر خطی که با `http(s)://` شروع شود به‌عنوان ساب اضافه و دریافت می‌شود (به بخش بعد).

**۵) SOCKS/HTTP پروکسی:** یا لینک `socks://user:pass@host:port#name` (و `socks5://`) را در «افزودن» بچسبانید، یا دکمه‌ی **«+ SOCKS/HTTP»** را بزنید و فرم را پر کنید (نوع، host، پورت، و در صورت نیاز نام‌کاربری/رمز). این پروکسی مثل هر کانفیگ دیگری قابل انتخاب، پینگ، زنجیره و استفاده در استخر است.

**۶) WireGuard دستی:** دکمه‌ی «+ WireGuard» → فرم را پر کنید.
> 💡 فیلد **Endpoint** باید آدرس عمومی سرور به شکل `host:port` باشد (مثل `de.example.com:51820`)، نه آدرس محلی تونل. فیلد **Address** آدرس محلی شما و حتماً `/32` است (مثل `10.8.0.2/32`). برای دسترسی به یک رنج خاص، آن را در **Allowed IPs** بگذارید (مثل `192.168.60.0/24`) یا `0.0.0.0/0` را نگه دارید.

> ⚡ **میان‌بر:** هرجای برنامه (بیرون از کادرهای متنی) **Ctrl+V** بزنید تا کانفیگ یا لینک ساب از کلیپ‌بورد فوراً اضافه شود.

ویرایش: روی آیکون ✎ هر سرور بزنید تا پروتکل، ترنسپورت، TLS/Reality، کلیدها و … را تغییر دهید.

---

## 📡 ساب‌اسکریپشن (Subscriptions)

در تب **ساب‌ها**:

- **افزودن ساب:** آدرس `https://...` و یک نام دلخواه بدهید.
- **به‌روزرسانی:** دستی (آیکون ⟳) یا **خودکار** با فاصله‌ی دلخواه (مثلاً هر ۶۰ دقیقه).
- **حجم و زمان:** اگر سرور ساب هدر استاندارد `Subscription-Userinfo` بفرستد، روی کارت ساب دو نوار نمایش داده می‌شود:
  - 🔵 **حجم مصرف:** «۱۲٫۳ GB / ۵۰ GB · ۲۵٪» با نوار رنگی (سبز → زرد → قرمز با نزدیک شدن به سقف).
  - 🟢 **زمان باقی‌مانده:** «۱۸ روز مانده» یا «منقضی شده».

> با حذف یک ساب، سرورهای متعلق به آن هم پاک می‌شوند؛ سرورهایی که دستی اضافه کرده‌اید دست‌نخورده می‌مانند.

---

## 🔌 حالت اتصال: پروکسی یا TUN (Connection Mode)

روی کارت حالت در صفحه‌ی اتصال بزنید تا بین دو حالت سوییچ کنید:

| حالت | چه‌چیزی تونل می‌شود؟ | نیاز به ادمین | کاربرد |
|---|---|---|---|
| ⚡ **پروکسی** | فقط برنامه‌هایی که از پروکسی سیستمی/SOCKS استفاده می‌کنند (مرورگرها، …) | خیر | سبک و سریع، اکثر کارهای روزمره |
| 🛡️ **TUN** | **کل ترافیک سیستم** (همه‌ی برنامه‌ها) | بله | وقتی برنامه‌ای پروکسی را نمی‌خواند یا تونل کامل لازم است |

> در حالت TUN روی ویندوز به `sing-box` (یا `tun2socks`) و `wintun.dll` و **دسترسی ادمین** نیاز است. اگر ادمین نباشید، برنامه پیشنهاد می‌دهد خودش را با دسترسی ادمین دوباره اجرا کند.
> روی مک، هنگام اتصال **یک‌بار** پنجره‌ی **رمز عبور** نمایش داده می‌شود و تونل کامل برپا می‌شود.

### لایه‌ی تونل: sing-box (یا tun2socks)

کارتِ شبکه‌ی مجازی را یکی از این دو می‌سازد. **تنظیمات → بک‌اندِ TUN**:

| بک‌اند | چه می‌کند |
|---|---|
| **sing-box** (پیش‌فرض، پیشنهادی) | خودش کارت `IRNetFree` را بالا می‌آورد و روت‌های **IPv4 و IPv6** را می‌گذارد (`auto_route`)، پس v6 از تونل بیرون نمی‌زند. IP سرورها و DNSهای bypass را از تونل مستثنی می‌کند. هنگام خروج (حتی با kill) **خودش روت‌ها و فیلترهایش را پاک می‌کند**. DNS کارت تونل روی `172.19.0.2` (و در حالت IPv6 روی `fdfe:dcba:9876::2`) می‌نشیند و از داخل تونل به هسته می‌رسد. |
| **tun2socks** (قدیمی) | همان کار با روت‌های `0.0.0.0/1` + `128.0.0.0/1`؛ فقط IPv4 و پاک‌سازی‌اش دست برنامه است. برای سازگاری نگه داشته شده. |

- **انتخاب واقعی:** اگر sing-box نصب باشد از آن استفاده می‌شود؛ اگر نه، به tun2socks برمی‌گردد و **در لاگ‌ها می‌نویسد چرا**. اگر خودتان `tun2socks` را انتخاب کنید، همان اجرا می‌شود.
- تغییر بک‌اند مثل بقیه‌ی تنظیماتِ تونل، **با اتصال مجدد** اعمال می‌شود (برنامه خودش می‌پرسد).

### گارد نشتی (Leak Guard)

خودِ TUN، *مسیر* را می‌گیرد ولی به‌تنهایی جلوی همه‌ی نشتی‌ها را نمی‌گیرد (کارت‌های شبکه‌ی فیزیکی DNS خودشان را دارند، WebRTC مستقیم UDP می‌زند). **تنظیمات → گارد نشتی** سه سطح دارد و **فقط در حالت TUN** معنی دارد:

| سطح | چه چیزی را می‌بندد |
|---|---|
| **خاموش** | هیچ؛ فقط خود TUN. |
| **استاندارد** (پیش‌فرض) | DNS **همه‌ی کارت‌های شبکه‌ی فیزیکی** (v4 و v6) روی آدرس تونل گذاشته می‌شود، پس هیچ برنامه‌ای نمی‌تواند از resolverِ مودم/ISP بپرسد. مقدارهای اصلی قبل از تغییر ذخیره و هنگام قطع/خروج برگردانده می‌شوند. |
| **سخت‌گیرانه** | همه‌ی موارد بالا + هر چیزی که **از تونل نمی‌رود** بسته می‌شود: DNS معمولی روی پورت ۵۳ بیرون از تونل، کل IPv6 بیرون از تونل، و UDPِ خارج از تونل (یعنی **WebRTC/STUN**). امن‌ترین حالت، ولی می‌تواند بازی‌ها و تماس‌های تصویری را بشکند. |

- **اگر برنامه کرش کند یا برق برود:** فایل `userData/tun-state.json` مقدارهای اصلی DNS را نگه داشته و **در اجرای بعدی خودکار برگردانده می‌شوند** (در لاگ‌ها نوشته می‌شود). لازم نیست دستی کاری کنید.
- **روی مک:** سطح **سخت‌گیرانه آزمایشی** است — با یک anchor از `pf` کار می‌کند که هنوز روی مک واقعی آزمایش نشده؛ خودِ رابط هم این را می‌گوید. سطح استاندارد (`networksetup -setdnsservers` روی همه‌ی سرویس‌ها) مسیر معمول است.

### بستن UDP در حالت پروکسی

قرینه‌ی گارد است برای وقتی که **TUN روشن نیست**: سوییچ **«بستن UDP در حالت پروکسی»** روی هر کارت شبکه‌ی فیزیکی UDPِ خروجی (به‌جز پورت ۵۳) را می‌بندد، تا WebRTC/STUN نتواند IP واقعی‌تان را لو بدهد — چون در حالت پروکسی، برنامه‌هایی که پروکسی سیستمی را نمی‌خوانند و همچنین UDP در هر صورت از کنار تونل رد می‌شوند. **بازی‌ها و تماس‌هایی که UDP لازم دارند از کار می‌افتند**، پس پیش‌فرض خاموش است. در حالت TUN این سوییچ غیرفعال نمایش داده می‌شود چون خودِ TUN، UDP را هم از تونل می‌برد.

---

## 🧭 روتینگ (Routing)

تب **روتینگ** چهار سطح کنترل می‌دهد:

### ۱) روتینگ ساده (Simple)
یکی از حالت‌ها را انتخاب کنید:
- **گلوبال:** همه از پروکسی (به‌جز شبکه‌ی محلی).
- **دور زدن ایران:** ترافیک داخلی مستقیم، بقیه از پروکسی.
- **دور زدن چین:** مشابه، برای سایت‌های چین.
- **مستقیم:** همه‌چیز مستقیم (برای تست).
به‌علاوه: **بلاک تبلیغات** و **Sniffing** (تشخیص دامنه برای روتینگ دقیق‌تر).
> حالت‌های «دور زدن» و «بلاک تبلیغات» به فایل‌های `geoip.dat` و `geosite.dat` نیاز دارند (از بخش فایل‌های موردنیاز دانلود کنید).

### ۲) قوانین سفارشی (Custom rules)
هر خط به شکل `نوع , مقدار , خروجی`:
```
domain, geosite:google, proxy
ip, 8.8.8.8, direct
domain, geosite:category-ads-all, block
port, 443, proxy
```
خروجی‌ها: `proxy` / `direct` / `block`.

### ۳) روتینگ پیشرفته گرافیکی (Advanced)
سوییچ «روتینگ پیشرفته» را روشن کنید و قانون بسازید. هر قانون: **نوع → مقدار → مقصد**.

- **نوع:** `IP` / `دامنه` / `پورت` / `پروسه`.
- **مقدار:**
  - برای IP و دامنه یک **لیست پیشنهادی geoip/geosite** هست؛ لازم نیست حفظ باشید — تایپ کنید یا انتخاب کنید (مثل `geoip:ir`، `geosite:google`، `geosite:openai`).
  - مثال‌های دستی: `1.2.3.0/24`، `example.com`، `443`.
- **مقصد:** یک **منوی قابل‌جستجو** (مثل صفحه‌ی هوم) که همه‌ی کانفیگ‌ها، زنجیره‌ها، `مستقیم` و `بلاک` را نشان می‌دهد — برای لیست‌های طولانی، کافی است اسم را تایپ کنید.
- **بقیه‌ی ترافیک:** مقصد پیش‌فرض برای هر چیزی که با قانون‌ها مطابقت نکند.

قانون‌ها **از بالا به پایین** بررسی می‌شوند و **اولین تطابق** برنده است. سپس روی صفحه‌ی اتصال گزینه‌ی 🧭 را انتخاب و وصل شوید.

**مثال:** ترافیک اوپن‌ای‌آی از سرور آمریکا، سایت‌های ایرانی مستقیم، بقیه از سرور آلمان:
```
دامنه   geosite:openai     →  US-Server
دامنه   geosite:category-ir →  مستقیم
(بقیه‌ی ترافیک)            →  DE-Server
```

#### «حالت روتینگ ساده هم اعمال شود»
تیکِ کنارِ مقصدِ پیش‌فرض. وقتی روشن باشد، همان حالتی که بالای صفحه انتخاب کرده‌اید (دور زدن ایران / دور زدن چین) **زیرِ** قانون‌های خودتان هم اعمال می‌شود — پس دیگر لازم نیست بین «روتینگ پیشرفته» و «دور زدن ایران» یکی را انتخاب کنید یا قانون‌های `geosite:category-ir` را دستی بنویسید.

ترتیب دقیق:

```
۱) قانون‌های خودتان        ← همیشه اول؛ رنج شرکت از وایرگارد می‌رود
۲) رنج‌های خصوصی/LAN       ← مستقیم
۳) حالت روتینگ ساده        ← فقط وقتی تیک روشن باشد (سایت‌های ایرانی → مستقیم)
۴) بقیه‌ی ترافیک           ← مقصد پیش‌فرضِ شما
```

DNS هم دنبال همین می‌رود: با تیکِ روشن و حالت «دور زدن ایران»، نام‌های ایرانی از resolver داخلی پرسیده می‌شوند. اگر حالت را روی **گلوبال** بگذارید چیزی اضافه نمی‌شود و رفتار دقیقاً مثل قبل است (پیش‌فرض: خاموش).

### ۴) روتینگ بر اساس پروسه (Process)
نوع قانون را روی **«پروسه»** بگذارید و از لیست برنامه‌های در حال اجرا یکی را انتخاب کنید (دکمه‌ی ⟳ برای تازه‌سازی لیست).

> ℹ️ **چطور کار می‌کند و محدودیت‌ها:** هسته‌ی Xray امکان تطبیق مستقیم «نام پروسه» را ندارد. بنابراین برنامه IPهای مقصدی را که آن پروسه به آن‌ها وصل می‌شود کشف کرده و آن‌ها را به مقصد انتخابی شما هدایت می‌کند (و در حافظه نگه می‌دارد). یعنی:
> - برای برنامه‌هایی با **سرور ثابت** (بازی، کلاینت اختصاصی، ابزار دیتابیس) بهترین نتیجه را دارد.
> - برنامه‌هایی که به IPهای مشترک CDN وصل می‌شوند (مثل مرورگر) قابل‌اعتماد نیست.
> - با روشن‌کردن «به‌روز نگه‌داشتن مسیر پروسه‌ها»، هر چند ثانیه مقصدهای جدید اضافه و هسته کوتاه ری‌لود می‌شود (پیش‌فرض خاموش = فقط هنگام اتصال). دکمه‌ی «پاک‌سازی IPهای یادگرفته‌شده» هم هست.

**مثال:** فقط ترافیک یک بازی (`game.exe`) از سرور با پینگ پایین، بقیه مستقیم.

---

## ⛓️ زنجیره پروکسی (Proxy Chains)

در تب **زنجیره** یک زنجیره بسازید: نام بدهید و چند سرور را **به‌ترتیب** بچینید (با کشیدن‌و‌رها کردن جابه‌جا می‌شوند). ترافیک از سرور اول → دوم → … → خروجی عبور می‌کند. هر زنجیره مثل یک «کانفیگ» در صفحه‌ی اتصال و در روتینگ پیشرفته قابل‌انتخاب است.

> 🔎 برای لیست بلند سرورها، بالای «سرورهای موجود» یک **کادر جستجو** هست و لیست **اسکرول‌دار** است.

**کاربرد رایج — دسترسی به دیتابیس از طریق WireGuard:** اگر WireGuard مستقیم وصل نمی‌شود، یک کانفیگ معمولی را **اول** و WireGuard را **آخر** زنجیره بگذارید؛ سپس در روتینگ پیشرفته رنج IP دیتابیس را به این زنجیره بفرستید:
```
IP   10.20.0.0/16   →  ⛓ DB-Chain   (Config → WireGuard)
```

---

## 🧩 استخر پروکسی (Multi‑Proxy Pool)

گاهی می‌خواهید **چند خروجی هم‌زمان** داشته باشید — مثلاً یک برنامه از سرور آلمان، یکی از سرور آمریکا و یکی از یک زنجیره — هرکدام روی **پورت محلی جدا**. تب **استخر پروکسی** دقیقاً همین کار را می‌کند:

- روی **«+ پروکسی جدید»** بزنید تا یک ردیف اضافه شود؛ برای هر ردیف:
  - **خروجی:** یک کانفیگ یا زنجیره (منوی قابل‌جستجو).
  - **پورت SOCKS:** پورت محلی این خروجی (خودکار از `60001` به بالا پیشنهاد می‌شود).
  - **پورت HTTP:** اختیاری.
  - سوییچ **فعال/غیرفعال**.
- سپس از دکمه‌ی **«اتصال استخر»** یا از صفحه‌ی اتصال گزینه‌ی 🧩 وصل شوید. یک نمونه‌ی Xray بالا می‌آید و **همه‌ی پورت‌ها هم‌زمان** فعال می‌شوند.
- در برنامه‌های خود، هرکدام را به پورت دلخواه وصل کنید:
  ```
  App A → SOCKS 127.0.0.1:60001   (خروجی: DE‑Server)
  App B → SOCKS 127.0.0.1:60002   (خروجی: US‑Chain)
  ```

**سازگاری با پروکسی و TUN:** پورت‌های استاندارد (SOCKS/HTTP تنظیمات) هم باز می‌شوند و به **«اولین پروکسیِ فعالِ استخر»** (به‌عنوان خروجی اصلی) وصل‌اند؛ بنابراین **پروکسی سیستمی، حالت TUN و بررسی IP** دقیقاً مثل حالت تک‌کانفیگ کار می‌کنند و پورت‌های استخر خروجی‌های *اضافه* روی آن هستند. در حالت TUN، IP سرورِ همه‌ی خروجی‌های استخر برای جلوگیری از حلقه، bypass می‌شوند.

> ⚠️ پورت‌ها نباید تکراری باشند (برنامه هنگام تخصیص خودکار این را رعایت می‌کند). اگر یک پورت اشغال باشد، Xray هنگام اتصال خطای واضح می‌دهد.

---

## 🧩 DNS

برنامه دو فهرست DNS دارد (هر دو در تنظیمات، با کاما یا از منوی آماده):

- **DNS خارجی** — برای همه‌ی دنیا، از داخل تونل. پیش‌فرض DoH است (`https://1.1.1.1/dns-query, https://8.8.8.8/dns-query`) چون سرورهایی که UDP را می‌بندند دیگر DNS را نمی‌شکنند. منوی آماده: Cloudflare / Google / Quad9 / AdGuard (DoH) و نسخه‌ی UDP آن‌ها. `host:port` هم پذیرفته می‌شود.
- **DNS داخلی** — فقط در حالت «دور زدن ایران» (یا قانون پیشرفته‌ی `geosite:category-ir → مستقیم`): دامنه‌های ایرانی از این‌جا و **مستقیم** resolve می‌شوند و جواب باید IP ایرانی باشد. پیش‌فرض شکن (`178.22.122.100, 185.51.200.2`)؛ منوی آماده: شکن / الکترو / بگذر / 403.
- **مدیریت DNS توسط برنامه** (پیش‌فرض روشن) — هر بسته‌ی DNS که به هسته برسد همان‌جا جواب داده می‌شود؛ در حالت TUN، DNS سیستم روی آدرس تونل قرار می‌گیرد و از تونل می‌رود. خاموش = رفتار قدیمی (فهرست خارجی همان‌طور که هست، بدون رهگیری).
- **IPv6** (پیش‌فرض خاموش) — خاموش یعنی هسته فقط IPv4 برمی‌گرداند تا برنامه‌ها سراغ آدرسی نروند که از تونل عبور نمی‌کند.

فهرست قدیمی `DNS` هنگام اجرای اول به این دو فهرست تبدیل می‌شود (IPهای عمومی شناخته‌شده به DoH، DNS های ایرانی به فهرست داخلی).

---

## 🌐 اشتراک در شبکه محلی (LAN)

سوییچ «اجازه به شبکه محلی» را روشن کنید تا این دستگاه به‌عنوان پروکسی برای بقیه‌ی دستگاه‌های شبکه عمل کند.

- به‌محض روشن‌کردن، **آدرس و پورت** نمایش داده می‌شود، مثل:
  `آدرس برای دستگاه‌های دیگر: 192.168.1.20:10809 (HTTP) • 192.168.1.20:10808 (SOCKS)`
- روی دستگاه دیگر، همان IP و پورت را به‌عنوان پروکسی HTTP یا SOCKS وارد کنید.
- روی ویندوز برای باز شدن **فایروال** به **دسترسی ادمین** نیاز است (برنامه خودش پورت‌ها را باز می‌کند).
- پس از تغییر این گزینه، یک‌بار دوباره وصل شوید تا اعمال شود.

---

## 🚨 کیل‌سوییچ (Kill Switch)

سوییچ «کیل‌سوییچ» را روشن کنید تا اگر اتصال **به‌طور ناگهانی** قطع شد، **کل اینترنت بسته شود** و هیچ ترافیکی بدون تونل نشت نکند.

- هنگام قطع ناگهانی، یک **بنر قرمز** ظاهر می‌شود با دو دکمه:
  - **اتصال مجدد** — دوباره وصل می‌شود.
  - **باز کردن اینترنت** — مسدودسازی را برمی‌دارد و به حالت عادی برمی‌گردد.
- قطع **عمدی** توسط خودتان کیل‌سوییچ را فعال نمی‌کند.
- روی ویندوز به **دسترسی ادمین** نیاز دارد (با فایروال کار می‌کند). اگر برنامه بسته یا کرش شود، قانون فایروال خودکار پاک می‌شود تا اینترنت دائمی بسته نماند.

---

## 🔐 اجرای با دسترسی ادمین (Run as Admin)

حالت TUN، کیل‌سوییچ و باز کردن فایروال LAN روی ویندوز نیازمند دسترسی ادمین‌اند. سه راه:
1. ترمینال/برنامه را **Run as administrator** اجرا کنید، سپس `npm start`.
2. از داخل برنامه دکمه/پیام **«اجرا با دسترسی ادمین»** را بزنید (خودش بسته و دوباره با ادمین باز می‌شود).
3. (برای تست توسعه، بدون ساخت exe) از PowerShell:
   ```powershell
   Start-Process ".\node_modules\electron\dist\electron.exe" -ArgumentList "<مسیر-کامل-پروژه>" -Verb RunAs
   ```

---

## 🍎 اولین اجرا روی مک (macOS — First Run)

> ⚠️ نیمه‌ی macOSِ حالت TUN، گارد نشتی و آماده‌سازی باینری‌ها **بدون دسترسی به یک مک واقعی** نوشته شده است. اگر چیزی کار نکرد، پیش از هر کار دیگری [اسکریپت خودآزما](#۵-اسکریپت-خودآزما-self-check) را اجرا کنید و **کل خروجی‌اش** را در ایشو بگذارید — سریع‌ترین راه رفع مشکل همین است.

### ۱) Gatekeeper — اولین باز کردن برنامه

برنامه با گواهی Apple امضا نشده، پس دابل‌کلیک ساده ممکن است با پیام «…can't be opened» رد شود. **فقط یک‌بار**:

- روی `IRNetFree.app` **راست‌کلیک → Open** بزنید و در دیالوگ روی **Open** تأیید کنید،
- یا در ترمینال:
  ```bash
  xattr -cr /Applications/IRNetFree.app
  ```

از دفعه‌ی بعد مثل هر برنامه‌ی دیگری باز می‌شود.

### ۲) دانلود هسته‌ها

تنظیمات → **فایل‌های موردنیاز** → `هسته Xray` و `هسته sing-box`. برنامه خودش معماری درست (Intel یا Apple Silicon) را می‌گیرد، صفت **quarantine** را از باینری پاک می‌کند و آن را **ad‑hoc امضا** می‌کند؛ روی Apple Silicon بدون این امضا باینری موقع اجرا با `Killed: 9` بسته می‌شود و خطایش هیچ ربطی به علت واقعی ندارد. اگر امضا شکست خورد، یک‌بار ابزارهای خط فرمان Xcode را نصب کنید و دوباره دانلود بزنید:

```bash
xcode-select --install
```

### ۳) اولین اتصال TUN — پنجره‌ی رمز عبور

حالت TUN روی مک به دسترسی root نیاز دارد، ولی **کل برنامه** با root اجرا نمی‌شود؛ فقط همان چند دستور در لحظه‌ی اتصال بالا می‌روند. پس ممکن است هنگام اتصال **تا دو پنجره‌ی رمز** ببینید:

1. یکی برای بالا آوردن خودِ تونل (`sing-box` به‌عنوان root)؛
2. و اگر **گارد نشتی** روی «استاندارد» باشد، یکی برای نوشتن DNS روی همه‌ی سرویس‌های شبکه.

اگر پنجره را Cancel کنید، اتصال با پیام «برای حالت TUN باید اجازه دسترسی (رمز عبور) بدهید» متوقف می‌شود — این یعنی همه‌چیز درست کار می‌کند، فقط اجازه ندادید.

> اگر برنامه **بد بسته شود** (کرش، قطع برق، Force Quit)، اجرای بعدی یک‌بار رمز می‌پرسد تا DNS اصلی را برگرداند و تونلِ جامانده را ببندد. در لاگ می‌نویسد چه کرد. این طبیعی است.

### ۴) چه چیزی روی مک آزمایشی است

| بخش | وضعیت |
|---|---|
| **TUN با sing-box** | مسیر اصلی. روت‌ها را خود sing-box با `auto_route` می‌گذارد و نام دستگاه را کرنل می‌دهد (`utun0`، `utun3`، …) — برنامه دستگاهِ تازه‌ظاهرشده را پیدا می‌کند و در لاگ می‌نویسد. |
| **گارد استاندارد** | با `networksetup -setdnsservers` روی همه‌ی سرویس‌های شبکه. مقدارهای اصلی پیش از تغییر در `tun-state.json` ذخیره می‌شوند. |
| **گارد سخت‌گیرانه** | **آزمایشی** — با یک anchor از `pf`؛ هنوز روی مک واقعی آزمایش نشده. خودِ تنظیمات هم این را می‌گوید. اگر شبکه‌تان قطع شد، گارد را روی «استاندارد» برگردانید. |
| **tun2socks** (بک‌اند قدیمی) | کار می‌کند ولی فقط IPv4 است؛ فقط وقتی سراغش بروید که sing-box مشکل داشته باشد. |

### ۵) اسکریپت خودآزما (Self-check)

```bash
npm run selfcheck:mac

# یا مستقیم، از پوشه‌ی پروژه:
bash scripts/mac-selfcheck.sh

# اگر می‌خواهید نسخه‌ی نصب‌شده را هم چک کند، مسیر .app را بدهید:
bash scripts/mac-selfcheck.sh /Applications/IRNetFree.app

# برای دیدن وضعیت فایروال pf (فقط این بخش به root نیاز دارد):
sudo bash scripts/mac-selfcheck.sh
```

اسکریپت **فقط می‌خواند**: هیچ تنظیمی را عوض نمی‌کند، هیچ پروسه‌ای را نمی‌کشد و هیچ تونلی بالا نمی‌آورد (تنها چیزی که می‌نویسد یک کانفیگ موقت برای `sing-box check` است که خودش پاکش می‌کند). هر بررسی **یک خط** چاپ می‌کند با برچسب `OK` / `WARN` / `FAIL`:

| بخش | چه چیزی را می‌بیند |
|---|---|
| **باینری‌ها** | وجود و اجراپذیریِ `xray`، `xray-pattn` و `sing-box` در پوشه‌ی داده‌ها (و داخل `.app` اگر مسیرش را بدهید)، وضعیت `codesign`، صفت quarantine، و خروجی `version` هرکدام |
| **شبکه** | دروازه و اینترفیس پیش‌فرض، نام سرویسِ `networksetup` آن اینترفیس، DNS فعلی‌اش، فهرست دستگاه‌های `utun`، و وضعیت `pf` |
| **کانفیگ TUN** | همان کانفیگی که برنامه می‌سازد را با `sing-box check` اعتبارسنجی می‌کند |
| **باقی‌مانده‌ها** | `tun-state.json`، پوشه‌های موقت `irnf-*`، و پروسه‌های `sing-box`/`tun2socks` که هنوز در حال اجرا هستند |

نمونه‌ی خروجی:

```
-- network --
OK    default route: gateway 192.168.1.1 via en0
OK    network service for en0: Wi-Fi
OK    DNS of Wi-Fi: automatic (DHCP)
OK    utun devices: utun0 utun1
WARN  pfctl needs root — re-run with: sudo bash scripts/mac-selfcheck.sh

-- summary --
OK 18 · WARN 2 · FAIL 0
```

> 📋 **هنگام گزارش مشکل، کل خروجی را از خط اول تا `summary` بچسبانید** — نه فقط خط‌های `FAIL`. خط‌های `OK` هم بخشی از تشخیص‌اند.

### ۶) چک‌لیست تأیید روی مک (Verification checklist)

بعد از اولین نصب، این‌ها را به‌ترتیب بزنید؛ هرکدام یک خط از طرحِ تأیید فاز ۳ است:

| ✔ | آزمون | انتظار |
|---|---|---|
| ☐ | `npm run selfcheck:mac` | هیچ خط `FAIL` ای نباشد |
| ☐ | اتصال در **حالت پروکسی** (بدون TUN) → «بررسی IP» | IP و کشورِ سرور |
| ☐ | **حالت TUN** با **بک‌اندِ TUN** = `sing-box` | وصل می‌شود؛ در لاگ `TUN device: utunN` و «حالت TUN فعال شد» |
| ☐ | زیر TUN، در ترمینال: `route -n get default` | خط `interface:` روی `utunN` باشد، نه `en0` |
| ☐ | زیر TUN: [ipleak.net](https://ipleak.net) | نه DNS مودم/ISP دیده شود، نه IPv6 (وقتی تنظیم **IPv6** خاموش است) |
| ☐ | **گارد نشتی** = `استاندارد` → `networksetup -getdnsservers Wi-Fi` | `172.19.0.2` (آدرس تونل) |
| ☐ | **گارد نشتی** = `سخت‌گیرانه` (آزمایشی) → [browserleaks.com/webrtc](https://browserleaks.com/webrtc) | فقط IP خروجی؛ نشانی محلی/واقعی نه |
| ☐ | زیر TUN **سرور را عوض کنید** | بعد از چند ثانیه مرور دوباره کار می‌کند (بدون قطع دستی) |
| ☐ | **قطع اتصال** → دوباره `networksetup -getdnsservers Wi-Fi` | مقدار اصلی برگشته (یا «There aren't any DNS Servers set…») |
| ☐ | بعد از قطع: `pgrep -fl sing-box` و `ifconfig -l` | نه پروسه‌ای مانده، نه `utun`ِ ما |
| ☐ | (اختیاری) **کرش‌تست**: برنامه را Force Quit کنید و دوباره بازش کنید | یک‌بار رمز می‌پرسد، DNS خودکار برمی‌گردد و در لاگ می‌نویسد |
| ☐ | مصرف CPU در حالت بی‌کار (Activity Monitor) | `IRNetFree` و هسته‌ها روی‌هم ≤ ۱٪ |

---

## 📦 فایل‌های موردنیاز (Required Files)

در **تنظیمات → فایل‌های موردنیاز**، هر مؤلفه با یک کلیک دانلود/به‌روزرسانی می‌شود (بدون ساخت دوباره‌ی برنامه):

| مؤلفه | کاربرد |
|---|---|
| **هسته Xray** | اجرای اصلی پروکسی/تونل (نسخه‌اش هم نمایش داده می‌شود) |
| **هسته Xray-PattN** | fork پترنیها، ساخته‌شده از شاخه‌ی `main` بالادست؛ کانفیگ‌های VLESS/Trojan بدون TLS (مثل ws روی پورت ۸۰ پشت CDN) را می‌پذیرد. ریلیز فعلی هسته‌ی رسمی هم آن‌ها را اجرا می‌کند، ولی هر وقت بالادست چکِ شاخه‌ی `main` را منتشر کند، برنامه خودکار روی این هسته اجرایشان می‌کند |
| **هسته sing-box** | بک‌اندِ پیش‌فرضِ **حالت TUN** (و ضد DPI) |
| **tun2socks** | حالت TUN — بک‌اندِ **قدیمی**، جایگزین وقتی sing-box نصب نیست |
| **wintun.dll** | حالت TUN (فقط ویندوز) — هر دو بک‌اند به آن نیاز دارند |
| **geoip + geosite** | روتینگ مبتنی بر کشور/دسته (دور زدن ایران/چین، بلاک تبلیغات، قوانین `geoip:`/`geosite:`) |

- برنامه به‌طور خودکار معماری درست (x64/arm64) را می‌گیرد و روی مک باینری‌ها را برای اجرا روی Apple Silicon امضای ad‑hoc می‌کند.
- دکمه‌ی **«حذف فایل‌های دانلودشده»** همه‌ی فایل‌هایی که برنامه دانلود کرده (در پوشه‌ی داده‌ها) را پاک می‌کند (نسخه‌ی همراهِ نصب دست‌نخورده می‌ماند).

**لایسنس مؤلفه‌های همراه (Bundled third-party binaries)** — باینری‌هایی که در `bin/` کامیت شده‌اند و با نصب‌کننده توزیع می‌شوند، متن لایسنس خودشان را کنارشان دارند:

| باینری | لایسنس | متن |
|---|---|---|
| [tun2socks](https://github.com/xjasonlyu/tun2socks) v2.6.0 | MIT | `bin/LICENSE-tun2socks.txt` |
| [Wintun](https://www.wintun.net/) | — | `bin/LICENSE-wintun.txt` |

هسته‌ی Xray، sing-box و فایل‌های geo در ریپو کامیت نشده‌اند و هنگام اجرا دانلود می‌شوند، پس لایسنس‌شان همراه خودشان می‌آید.

---

## 🆙 نسخه و به‌روزرسانی (Version & Update)

در **تنظیمات → درباره و به‌روزرسانی**:
- **نسخه‌ی برنامه** نمایش داده می‌شود.
- دکمه‌ی **«بررسی به‌روزرسانی»** نسخه‌ی فعلی را با آخرین ریلیز گیت‌هاب مقایسه می‌کند؛ اگر نسخه‌ی جدیدی باشد، دکمه‌ی **«دانلود»** صفحه‌ی ریلیز را باز می‌کند.
- **نسخه‌ی هسته‌ی Xray** هم در همین صفحه و کنار «فایل‌های موردنیاز» نشان داده می‌شود.

---

## 📊 آمار و تست (Stats & Testing)

- **پینگ TCP:** زمان handshake تا سرور (سریع).
- **تأخیر واقعی:** یک نمونه‌ی موقتی Xray بالا می‌آید و تأخیر واقعی از داخل تونل اندازه گرفته می‌شود (برای زنجیره هم کار می‌کند).
- **بررسی IP:** IP و کشور خروجی (مستقیم یا از طریق پروکسی).
- **آمار زنده:** سرعت و حجم دانلود/آپلود لحظه‌ای + مجموع این نشست.
- **تست همه:** پینگ هم‌زمان همه‌ی سرورها/زنجیره‌ها.

### تست‌های توسعه‌دهنده

```bash
npm test            # تست‌های واحد (بدون نیاز به هسته)
npm run validate    # هر شکلِ کانفیگی که ساخته می‌شود را به هسته می‌دهد: `xray run -test`
npm run probe:wg    # سناریوی وایرگارد شرکتی، سرتاسری، با یک peer واقعی
```

`npm run validate` ثابت می‌کند هسته کانفیگ را **قبول** می‌کند؛ `npm run probe:wg` ثابت می‌کند واقعاً **کار** می‌کند: یک peer واقعی وایرگارد (با sing-box) بالا می‌آید، یک DNS شرکتی و یک سایت داخلی پشتِ آن قرار می‌گیرند، و برنامه با کانفیگِ خودش یک نام داخلی را از داخلِ تونل resolve و صفحه را دریافت می‌کند (و ۲ مگابایت هم منتقل می‌کند). همه‌چیز روی loopback است و به مسیرها/آداپتور/فایروال دست نمی‌زند.

```bash
IRNF_SINGBOX_EXE=/path/to/sing-box IRNF_XRAY_EXE=/path/to/xray-pattn npm run probe:wg
```

با `IRNF_XRAY_EXE` هستهٔ دوم (مثلاً فورک patterniha) هم با همان سناریو تست می‌شود، تا اختلاف رفتار دو هسته اینجا معلوم شود نه در گزارش کاربر.

---

## 🎨 ظاهر و رابط کاربری (UI)

- **رنگ‌بندی وضعیت:** هنگام اتصال، **تولبار و لوگو سبز** (با ✓) و هنگام قطع، **آبی با ✕ قرمز** می‌شوند.
- **نشان روی تسک‌بار ویندوز:** آیکون برنامه در نوار وظیفه نشان سبز (متصل) یا قرمز (قطع) می‌گیرد.
- **دکمه‌های پنجره:** کوچک‌کردن، **بزرگ‌نمایی/بازگردانی** (دکمه‌ی □)، و بستن به سینی سیستم (✕).
- **سینی سیستم (Tray):** نمایش/خروج/قطع اتصال از منوی tray.
- **زبان:** فارسی (RTL) و English، با یک کلیک.

---

## ⚙️ مرجع تنظیمات (Settings Reference)

| تنظیم | پیش‌فرض | توضیح |
|---|---|---|
| پورت SOCKS | `10808` | پورت inbound SOCKS5 |
| پورت HTTP | `10809` | پورت inbound HTTP |
| DNS خارجی | `https://1.1.1.1/dns-query, https://8.8.8.8/dns-query` | از داخل تونل |
| DNS داخلی | `178.22.122.100, 185.51.200.2` | مستقیم، فقط برای دامنه‌های ایرانی |
| مدیریت DNS | روشن | هسته هر DNS ای را که به آن برسد جواب می‌دهد |
| IPv6 | خاموش | فقط IPv4 |
| سطح لاگ | `warning` | none/error/warning/info/debug |
| پروکسی سیستمی | روشن | تنظیم خودکار پروکسی سیستم هنگام اتصال |
| حالت TUN | خاموش | تونل کل سیستم |
| بک‌اندِ TUN | `sing-box` | `sing-box` (پیشنهادی) یا `tun2socks` (قدیمی) |
| گارد نشتی | `استاندارد` | خاموش / استاندارد (DNS همه‌ی کارت‌ها از تونل) / سخت‌گیرانه (هر چه از تونل نمی‌رود بسته می‌شود) — فقط حالت TUN |
| بستن UDP در حالت پروکسی | خاموش | جلوی WebRTC/STUN را می‌گیرد وقتی TUN روشن نیست |
| اجازه به LAN | خاموش | اشتراک پروکسی در شبکه محلی |
| کیل‌سوییچ | خاموش | بستن اینترنت هنگام قطع ناگهانی |
| بلاک تبلیغات | روشن | بلاک `geosite:category-ads-all` |
| Sniffing | روشن | تشخیص دامنه برای روتینگ دقیق‌تر |
| به‌روزرسانی خودکار ساب | روشن | با فاصله‌ی دلخواه (پیش‌فرض ۶۰ دقیقه) |
| زبان | `fa` | fa / en |

> تنظیمات و کانفیگ‌ها فقط به‌صورت محلی در `userData/store.json` ذخیره می‌شوند.

---

## 🏗️ ساخت نسخه نصبی (Build)

**ویندوز:**
```bash
npm run dist          # NSIS installer + portable در پوشه‌ی dist/
```

**مک (Intel + Apple Silicon) — فقط روی خودِ macOS:**
```bash
npm run gen-icons     # یک‌بار، اگر assets/icon.icns نباشد
npm run dist:mac          # هر دو معماری
npm run dist:mac:x64      # فقط Intel
npm run dist:mac:arm64    # فقط M1/M2/M3/M4
```
خروجی: `IRNetFree-<version>-x64.dmg` و `IRNetFree-<version>-arm64.dmg` (و `.zip`).
> ⚠️ بیلد مک روی ویندوز ساخته نمی‌شود (به `hdiutil`/`codesign` نیاز است).

**اولین اجرا روی مک:** چون بدون گواهی Apple امضا شده، یک‌بار روی برنامه **راست‌کلیک → Open** بزنید یا `xattr -cr /Applications/IRNetFree.app` — جزئیات و چک‌لیست در [اولین اجرا روی مک](#-اولین-اجرا-روی-مک-macos--first-run).

**لینوکس:**
```bash
npm run dist:linux    # AppImage + deb
```

---

## 🐧 نسخه CLI / سرور بدون GUI (لینوکس، Headless)

روی یک سرور لینوکسی که **دسکتاپ/GUI ندارد** (VPS، سرور ابری، کانتینر…)، همین برنامه را به‌صورت **بدون واسط گرافیکی** اجرا کن. برنامه یک **وب‌سرور سبک** روی یک پورت محلی بالا می‌آورد که **دقیقاً همان رابط کاربری نسخه‌ی ویندوز/دسکتاپ** را نشان می‌دهد و **تمام امکانات** را دارد:

> پروکسی SOCKS/HTTP • **استخر پروکسی چندپورتی** • زنجیره‌ی پروکسی • ساب‌اسکریپشن (با به‌روزرسانی خودکار) • روتینگ ساده/پیشرفته/پروسه • حالت TUN • آمار زنده‌ی ترافیک • پینگ TCP و تأخیر واقعی • بررسی IP خروجی • مدیریت فایل‌های موردنیاز (xray/geo).

چرا سبک است؟ چون بک‌اند برنامه **هیچ وابستگی npm ندارد** و فقط با **Node.js core** کار می‌کند؛ پس نصب یعنی صرفاً «Node + سورس + باینری xray».

### 🧠 چطور کار می‌کند؟
به‌جای IPC داخلی Electron، یک پل ساده استفاده می‌شود:

```
مرورگر تو  ──HTTP/SSE──►  سرور Node روی سرور لینوکس  ──►  xray-core
(همان UI)      (پورت وب)        (src/server/*)            (پروکسی/تونل)
```

- `src/server/server.js` — وب‌سرور خالصِ Node: مسیر `POST /rpc` (فراخوانی توابع) + `GET /events` (رویدادهای زنده با SSE: لاگ/وضعیت/آمار) + سرو فایل‌های UI.
- `src/server/service.js` — همان منطق `main.js` دسکتاپ، بدون Electron (بازاستفاده‌ی کامل از ماژول‌های `src/main/*`).
- `src/server/web-api.js` — همان `window.api` را در مرورگر می‌سازد (با `fetch` و `EventSource`).

نتیجه: کدِ UI یکی است، رفتار یکی است؛ فقط لایه‌ی انتقال فرق دارد.

---

### 1) پیش‌نیازها
- یک سرور لینوکسی با دسترسی SSH (بدون نیاز به GUI).
- معماری CPU: `x86_64`/`amd64`، `arm64`/`aarch64` یا `armv7`.
- **نیازی به نصب دستی Node نیست** — اگر Node 18+ روی سیستم نباشد، اسکریپت نصب خودش یک نسخه‌ی portable دانلود می‌کند.
- برای حالت **پروکسی/استخر** نیازی به root **نیست**. فقط حالت **TUN** به root/sudo نیاز دارد.

---

### 2) نصب تک‌خطی (روی سرور)
روی خودِ سرور (از طریق SSH) این را بزن:
```bash
curl -fsSL https://raw.githubusercontent.com/sadrazkh/Irnetfree_xray-client/main/install.sh | bash
```
این اسکریپت به‌ترتیب:
1. اگر **Node.js 18+** نبود، یک Node **portable** مناسب معماری‌ات دانلود و کنار برنامه می‌گذارد (بدون دست‌زدن به سیستم).
2. سورس برنامه را از شاخه‌ی `main` می‌گیرد و در `~/.irnetfree/app` باز می‌کند.
3. **xray-core** را در `bin/` دانلود می‌کند.
4. یک لانچر آماده در `~/.irnetfree/run.sh` می‌سازد و دستور اجرا + راهنمای فوروارد پورت را چاپ می‌کند.

**فلگ‌های نصب** (با الگوی `| bash -s -- <flags>`):

| فلگ | کار |
|---|---|
| `--start` | بعد از نصب، بلافاصله سرور را اجرا کن (foreground) |
| `--service` | نصب و فعال‌سازی **سرویس systemd** (اجرای دائم + ری‌استارت خودکار؛ نیازمند sudo) |
| `--port <n>` | پورت وب UI (پیش‌فرض `6969`) |
| `--host <addr>` | آدرس bind (پیش‌فرض `127.0.0.1`؛ برای expose روی شبکه: `0.0.0.0`) |
| `--dir <path>` | مسیر نصب (پیش‌فرض `~/.irnetfree`) |

نمونه‌ها:
```bash
# نصب و اجرای فوری روی پورت 8080:
curl -fsSL https://raw.githubusercontent.com/sadrazkh/Irnetfree_xray-client/main/install.sh | bash -s -- --start --port 8080

# نصب دائمی به‌عنوان سرویس systemd (بالا می‌ماند و بعد ریبوت خودکار اجرا می‌شود):
curl -fsSL https://raw.githubusercontent.com/sadrazkh/Irnetfree_xray-client/main/install.sh | bash -s -- --service
```

---

### 3) اجرا
اگر بدون `--start`/`--service` نصب کردی، سرور را دستی اجرا کن:
```bash
~/.irnetfree/run.sh                    # روی 127.0.0.1:6969
~/.irnetfree/run.sh --port 8080        # پورت دلخواه
~/.irnetfree/run.sh --host 0.0.0.0     # expose روی شبکه (توکن اجباری می‌شود)
```
اگر پوشه‌ی `~/.local/bin` در PATH‌ات باشد، یک میان‌بر هم ساخته می‌شود:
```bash
irnetfree --port 6969
```

هنگام اجرا، خروجی چیزی شبیه این است:
```
  IRNetFree server (headless) — v0.9
  Data dir : /home/user/.local/share/irnetfree
  Listening: http://127.0.0.1:6969/
  ...ssh -N -L 6969:127.0.0.1:6969 user@SERVER...
```

---

### 4) دسترسی از روی لپ‌تاپ (فوروارد پورت) — گام‌به‌گام
سرور به‌طور **پیش‌فرض فقط روی `127.0.0.1`** گوش می‌دهد (امن‌ترین حالت؛ از بیرون قابل دسترسی نیست). برای دیدن پنل، پورت را با SSH به لپ‌تاپت **tunnel** کن:

**۱.** روی سرور، سرور را اجرا کن (یا سرویس systemd روشن باشد).
**۲.** روی **لپ‌تاپ خودت** یک ترمینال باز کن و بزن:
```bash
ssh -N -L 6969:127.0.0.1:6969 user@YOUR_SERVER_IP
```
（`-N` یعنی فقط تونل، بدون شل. این ترمینال را باز نگه دار.）
**۳.** حالا در **مرورگر لپ‌تاپت** برو به:
```
http://127.0.0.1:6969/
```
همین! پنل کامل بالا می‌آید — انگار برنامه‌ی دسکتاپ را باز کرده‌ای.

> 💡 روی ویندوز، هم `ssh` داخلیِ ویندوز کار می‌کند، هم PuTTY (در PuTTY: Connection → SSH → Tunnels → Source `6969`, Destination `127.0.0.1:6969`).

---

### 5) استفاده از پنل (همان کارهای دسکتاپ)
پس از باز شدن پنل روی مرورگر:

1. **افزودن کانفیگ:** تب **سرورها** → «+ افزودن» و لینک `vless://` / `vmess://` / `trojan://` / `ss://` / `socks://` را بچسبان (یا «+ SOCKS/HTTP» / «+ وایرگارد»). لینک ساب‌اسکریپشن (`https://…`) هم در تب **ساب‌ها** اضافه می‌شود.
2. **اتصال ساده:** تب **اتصال** → سرور را انتخاب و دکمه‌ی روشن/خاموش را بزن. با «بررسی IP» مطمئن شو.
3. **استخر پروکسی (چند خروجی هم‌زمان):** تب **استخر پروکسی** → «+ پروکسی جدید» → برای هر ردیف یک خروجی و یک **پورت SOCKS** (مثلاً `60001`, `60002`, …) بگذار → «اتصال استخر». حالا چند پروکسی هم‌زمان روی پورت‌های جدا داری.
4. **استفاده از پورت‌های پروکسی روی سرور:** پورت‌های SOCKS/HTTP (پیش‌فرض `10808`/`10809` و پورت‌های استخر مثل `60001`) روی **`127.0.0.1` سرور** باز می‌شوند. دو راه استفاده:
   - **از روی خود سرور:** برنامه‌ها را به `127.0.0.1:10808` (یا پورت استخر) وصل کن؛ مثلاً:
     ```bash
     curl --socks5-hostname 127.0.0.1:10808 https://ifconfig.me
     export ALL_PROXY=socks5://127.0.0.1:60001   # اپ‌های دیگر
     ```
   - **از روی لپ‌تاپ:** همان پورت پروکسی را هم SSH-forward کن و در مرورگر/برنامه‌ات به‌عنوان پروکسی SOCKS بگذار:
     ```bash
     ssh -N -L 10808:127.0.0.1:10808 -L 60001:127.0.0.1:60001 user@YOUR_SERVER
     # سپس پروکسی SOCKS5 برنامه = 127.0.0.1:10808  (یا 60001)
     ```
   - **در دسترس گذاشتن مستقیم روی شبکه:** در تنظیمات، «اجازه به شبکه محلی (LAN)» را روشن کن تا پورت‌های پروکسی روی `0.0.0.0` سرور باز شوند (فقط اگر فایروال/شبکه‌ات امن است).

> نکته: در نسخه‌ی سرور، «پروکسی سیستمی» پیش‌فرض **خاموش** است (سرور رابط گرافیکی ندارد که پروکسی سیستمی برایش معنی داشته باشد). استفاده‌ی اصلی، همان پورت‌های SOCKS/HTTP و استخر است.

---

### 6) سرویس دائمی با systemd
اگر با `--service` نصب کردی، سرویس `irnetfree` ساخته و فعال شده. مدیریتش:
```bash
systemctl status irnetfree      # وضعیت
systemctl restart irnetfree     # ری‌استارت
systemctl stop irnetfree        # توقف
journalctl -u irnetfree -f      # دیدن لاگ زنده
```
اگر بعداً خواستی پورت/هاست سرویس را عوض کنی، فایل `/etc/systemd/system/irnetfree.service` را ویرایش کن (خط `ExecStart`)، بعد:
```bash
sudo systemctl daemon-reload && sudo systemctl restart irnetfree
```

### اجرای دستی در پس‌زمینه (بدون systemd)
```bash
nohup ~/.irnetfree/run.sh --port 6969 >~/.irnetfree/server.log 2>&1 &
# توقف:  pkill -f src/server/server.js
```

---

### 7) اجرای مستقیم از سورس (بدون اسکریپت نصب)
اگر ریپو را کلون کرده‌ای:
```bash
npm run get-xray                              # یک‌بار، برای دانلود هسته xray
npm run server -- --port 6969 --host 127.0.0.1
# یا مستقیم:
node src/server/server.js --port 6969
```

---

### 8) امنیت 🔒
- **bind پیش‌فرض `127.0.0.1`** است؛ یعنی پنل فقط از طریق **SSH-forward** در دسترس است و از اینترنت باز نیست. این امن‌ترین و توصیه‌شده‌ترین حالت است.
- اگر با `--host 0.0.0.0` روی شبکه/اینترنت expose کنی، **توکن اجباری** می‌شود:
  - اگر `--token` ندهی، یک توکن تصادفی ساخته و در URL چاپ می‌شود (`http://IP:PORT/?token=...`).
  - می‌توانی خودت بدهی: `--token 'یک‌رشته‌ی‌قوی'`.
  - **هر** درخواست بدون توکن درست (`?token=` یا هدر `x-irnetfree-token`) رد می‌شود — حتی خودِ صفحه.
- چون این پنل به هسته‌ی VPN دسترسی دارد، **هرگز** بدون توکن (و ترجیحاً بدون HTTPS/reverse-proxy) مستقیم روی اینترنت بازش نکن. برای دسترسی از راه دور، **SSH-forward** بهترین گزینه است.

**همه‌ی گزینه‌های خط فرمان:**

| گزینه | پیش‌فرض | توضیح |
|---|---|---|
| `--port <n>` | `6969` | پورت وب UI |
| `--host <addr>` | `127.0.0.1` | آدرس bind (`0.0.0.0` برای شبکه) |
| `--token <secret>` | — | توکن دسترسی (روی bind غیرلوکال اجباری) |
| `--data-dir <path>` | `~/.local/share/irnetfree` | محل ذخیره‌ی کانفیگ‌ها/تنظیمات |
| `--no-auth` | — | غیرفعال‌کردن توکن حتی روی bind عمومی (خطرناک) |

---

### 9) به‌روزرسانی و حذف
**به‌روزرسانی** (دوباره اجرای همان نصب‌کننده؛ داده‌ها در `data-dir` جدا هستند و پاک نمی‌شوند):
```bash
curl -fsSL https://raw.githubusercontent.com/sadrazkh/Irnetfree_xray-client/main/install.sh | bash
sudo systemctl restart irnetfree   # اگر سرویس داری
```
**حذف کامل:**
```bash
sudo systemctl disable --now irnetfree 2>/dev/null; sudo rm -f /etc/systemd/system/irnetfree.service
rm -rf ~/.irnetfree                 # برنامه + Node portable
rm -rf ~/.local/share/irnetfree     # داده‌ها/تنظیمات (اگر می‌خواهی پاک شوند)
```

---

### 10) عیب‌یابی سرور
| مشکل | راه‌حل |
|---|---|
| **مرورگر باز نمی‌شود / Connection refused** | مطمئن شو سرور روی سرور در حال اجراست و SSH-forward برقرار است؛ پورت لوکال و ریموت را چک کن. |
| **`Port ... already in use`** | پورت دیگری بده: `--port 6970`. |
| **«هسته Xray پیدا نشد»** | `cd ~/.irnetfree/app && node scripts/download-xray.js` (یا از پنل: تنظیمات → فایل‌های موردنیاز → دانلود). |
| **روی bind عمومی صفحه `unauthorized` می‌دهد** | با توکن باز کن: `http://IP:PORT/?token=...` (توکن در خروجی سرور چاپ شده). |
| **حالت TUN کار نمی‌کند** | سرور را با `sudo` اجرا کن (حالت TUN **root** می‌خواهد) و `sing-box` را دانلود کن (یا `tun2socks` را)؛ برای اکثر کاربردها **پروکسی/استخر** کافی و بی‌دردسرتر است. |
| **آمار/رویدادها آپدیت نمی‌شود** | مرورگر باید از `EventSource`(SSE) پشتیبانی کند (همه‌ی مرورگرهای مدرن می‌کنند)؛ اگر پشت reverse-proxy هستی، بافرینگ SSE را خاموش کن. |

---

## 🤖 نسخه اندروید (Android)

نسخه‌ی اندروید یک **اپ نیتیو جدا (Kotlin + Jetpack Compose)** در پوشه‌ی [`android/`](android) است (کلاینت دسکتاپ Electron روی اندروید اجرا نمی‌شود). این اپ همان منطق کانفیگ‌سازیِ دسکتاپ را دارد (تک‌کانفیگ، **زنجیره**، **استخر پروکسی**، **SOCKS/HTTP**، روتینگ) و ترافیک را با **`VpnService` + `hev-socks5-tunnel`** به هسته‌ی **xray-core** (از طریق `libv2ray`) تونل می‌کند.

**ساخت محلی:**
```bash
cd android
bash scripts/fetch-libs.sh   # دانلود libv2ray.aar (لازم برای کامپایل) + در صورت تنظیم، .soی tun2socks
gradle wrapper               # یک‌بار، اگر ./gradlew نداری (Wrapper در گیت نگه‌داری نمی‌شود)
./gradlew assembleRelease    # خروجی: app/build/outputs/apk/release/*.apk
# یا برای دیباگ:
./gradlew assembleDebug
```
> در Android Studio کافی است پوشه‌ی `android/` را باز کنی (Sync خودش وابستگی‌ها را می‌گیرد؛ فقط `fetch-libs.sh` را یک‌بار اجرا کن تا `libv2ray.aar` سر جایش باشد).

- **حداقل نسخه:** Android 8.0 (API 26). **هدف:** API 34.
- دو مؤلفه‌ی نیتیو (`libv2ray.aar` و `libhev-socks5-tunnel.so`) در گیت نگه‌داری **نمی‌شوند**؛ اسکریپت `fetch-libs.sh` (و CI) آن‌ها را از ریلیزهای بالادست دانلود می‌کند.
- امضای ریلیز: اگر رازهای keystore در گیت‌هاب تنظیم شده باشند، APKِ **امضاشده** ساخته می‌شود؛ در غیر این صورت APKِ **دیباگ** برای تست تولید می‌شود (به بخش بعد).

> ⚠️ رفتار واقعیِ تونل روی **دستگاه/شبیه‌ساز** باید تست شود؛ نسخه‌ی دقیق `libv2ray` باید با امضای API در سرویس VPN بخواند (در صورت تغییر API بالادست، فقط `XrayVpnService.kt` نیاز به تطبیق دارد).

---

## 🚀 انتشار خودکار (GitHub Actions Release)

با **push کردن یک تگ** به شکل `v*` (مثلاً `v0.9.0`)، ورک‌فلو [`.github/workflows/release.yml`](.github/workflows/release.yml) هم **دسکتاپ** و هم **APK اندروید** را می‌سازد و همه را به همان **GitHub Release** پیوست می‌کند:

```bash
git tag v0.9.0
git push origin v0.9.0
```

خروجی‌های ریلیز:
- ویندوز: `IRNetFree-Setup-<v>.exe` و `IRNetFree-Portable-<v>.exe`
- لینوکس: `IRNetFree-<v>-x86_64.AppImage` و `.deb`
- **اندروید: `IRNetFree-<v>.apk`**
- (مک روی یک ranner جدا اگر فعال باشد)

**امضای اندروید (اختیاری ولی توصیه‌شده):** برای APKِ امضاشده‌ی release، این رازها را در گیت‌هاب (`Settings → Secrets and variables → Actions`) بگذارید:

| Secret | توضیح |
|---|---|
| `ANDROID_KEYSTORE_BASE64` | فایل keystore به‌صورت base64 (`base64 -w0 my.keystore`) |
| `ANDROID_KEYSTORE_PASSWORD` | رمز keystore |
| `ANDROID_KEY_ALIAS` | نام alias کلید |
| `ANDROID_KEY_PASSWORD` | رمز کلید |

اگر این رازها نباشند، CI به‌جای شکست، **APKِ دیباگ** را می‌سازد و پیوست می‌کند تا همیشه یک فایلِ قابل‌نصب داشته باشید.

---

## 📁 ساختار پروژه (Project Structure)

```
src/
  main/
    main.js          # فرآیند اصلی Electron، IPC، چرخه‌ی عمر، LAN/کیل‌سوییچ/overlay
    parser.js        # تبدیل لینک‌ها (vless/vmess/trojan/ss/socks/wireguard) به outbound اکس‌ری
    configBuilder.js # ساخت config.json کامل (inbounds + routing + حالت pool چندپورتی)
    xrayManager.js   # مدیریت پروسه‌ی xray-core (start/stop/test/version)
    engines.js       # رجیستری هسته‌ها: xray (رسمی)، xray-pattn (پترنیها)، sing-box
    engineChoice.js  # کدام هسته یک plan را اجرا می‌کند (انتخاب کانفیگ / هسته‌ی پیش‌فرض)
    assets.js        # وضعیت فایل‌های موردنیاز (مشترک دسکتاپ و headless)
    subscription.js  # ساب‌اسکریپشن + خواندن حجم/زمان از هدر Subscription-Userinfo
    tunManager.js    # حالت TUN (Windows wintun / macOS utun / Linux)
    procRouter.js    # کشف پروسه‌ها و IPهایشان برای روتینگ پراسس
    stats.js         # آمار زنده‌ی ترافیک
    sysproxy.js      # پروکسی سیستمی (Win/macOS/Linux)
    netutils.js      # پینگ TCP، تأخیر واقعی، بررسی IP/geo
    downloader.js    # دانلود/به‌روزرسانی هسته و فایل‌های جانبی
    store.js         # ذخیره‌ساز JSON
  preload/preload.js # پل امن IPC
  renderer/          # رابط کاربری (index.html / app.js / styles.css / i18n.js)
  server/            # نسخه‌ی headless (لینوکس/CLI): همان بک‌اند روی وب
    service.js       # هسته‌ی بدون‌Electron (بازاستفاده از همه‌ی ماژول‌های main/)
    server.js        # وب‌سرور HTTP + RPC + SSE (فقط Node core)
    web-api.js       # پلِ window.api در مرورگر (fetch/EventSource به‌جای IPC)
install.sh           # نصب‌کننده‌ی تک‌خطی لینوکس (Node + سورس + xray + لانچر/systemd)
scripts/
  download-xray.js   # دانلودر هسته
  mac-selfcheck.sh   # خودآزمای macOS (فقط‌خواندنی؛ خروجی‌اش را در ایشو بچسبانید)
android/             # اپ نیتیو اندروید (Kotlin + Compose)
  app/src/main/java/com/irnetfree/vpn/
    core/            # LinkParser, ConfigBuilder (پورت‌شده از JS، شامل pool/socks), Models, Store
    vpn/             # XrayVpnService (VpnService + libv2ray + hev-socks5-tunnel)
    ui/              # صفحه‌های Compose (سرورها / استخر / اتصال)
  scripts/fetch-libs.sh   # دانلود libv2ray.aar + libhev-socks5-tunnel .so
.github/workflows/
  release.yml        # ساخت دسکتاپ + APK اندروید روی تگ و پیوست به Release
  test.yml           # اجرای تست‌های واحد روی لینوکس/ویندوز/مک در هر push و PR
```

---

## 🔒 امنیت (Security)

- `contextIsolation` فعال و `nodeIntegration` غیرفعال؛ رندرر فقط از طریق preload با main حرف می‌زند.
- هنگام خروج/قطع، پروکسی سیستمی، روت‌های TUN، قانون LAN و کیل‌سوییچ **خودکار پاک‌سازی** می‌شوند.
- کلیدها و کانفیگ‌ها فقط **محلی** ذخیره می‌شوند؛ چیزی به سروری ارسال نمی‌شود (به‌جز خود ساب‌ها که شما اضافه می‌کنید).

---

## 🛠️ عیب‌یابی (Troubleshooting)

| مشکل | راه‌حل |
|---|---|
| **«هسته Xray پیدا نشد»** | تنظیمات → فایل‌های موردنیاز → دانلود (یا `npm run get-xray`). |
| **وصل شد ولی اینترنت ندارید** | حالت روتینگ را روی «گلوبال» بگذارید؛ پورت‌ها و سرور را چک کنید؛ «بررسی IP» را بزنید. |
| **حالت TUN کار نمی‌کند (ویندوز)** | برنامه را با **ادمین** اجرا کنید؛ مطمئن شوید `sing-box` (یا `tun2socks`) و `wintun.dll` نصب‌اند — لاگ می‌گوید کدام بک‌اند اجرا شده. |
| **TUN روی مک کار نمی‌کند** | هنگام اتصال رمز عبور را بدهید؛ `sing-box` (یا tun2socks) را از «فایل‌های موردنیاز» دانلود کنید؛ پیش از خروج، اول **قطع** کنید. اگر باز هم نشد: `npm run selfcheck:mac` و خروجی کامل را گزارش کنید ([راهنما](#-اولین-اجرا-روی-مک-macos--first-run)). |
| **DNS بعد از کرش عوض مانده** | برنامه را دوباره باز کنید؛ گارد نشتی مقدارهای اصلی را از `userData/tun-state.json` برمی‌گرداند و در لاگ می‌نویسد. |
| **روتینگ geoip/geosite بی‌اثر است** | فایل‌های `geoip.dat` و `geosite.dat` را دانلود کنید. |
| **LAN کار نمی‌کند** | برنامه را با ادمین اجرا کنید (فایروال)؛ روی دستگاه دیگر IP:پورت درست را بزنید؛ یک‌بار دوباره وصل شوید. |
| **کیل‌سوییچ فعال نمی‌شود** | با **ادمین** اجرا کنید (با فایروال کار می‌کند). |
| **اینترنت بعد از کرش بسته ماند** | برنامه را دوباره باز کنید؛ هنگام شروع، قانون کیل‌سوییچ پاک می‌شود. |
| **ساب دریافت نمی‌شود** | آدرس باید با `http(s)://` شروع شود و در دسترس باشد. |
| **پروکسی سیستمی پاک نشد** | برنامه را دوباره باز کنید؛ هنگام شروع پاک‌سازی می‌شود. |

---

<div align="center">

ساخته‌ی گروه **IRNetFree** — با ❤️ برای اینترنت آزاد
Built with **Electron** + **xray‑core** · License: MIT

</div>
